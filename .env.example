# Copy this file to .env and fill in real values. NEVER commit .env to git.

# App
PORT=3000
SESSION_SECRET=change-me-to-a-long-random-string

# Database (local dev -> localhost; on AWS -> your RDS endpoint)
DB_HOST=localhost
DB_PORT=3306
DB_USER=musafir
DB_PASSWORD=musafirpass
DB_NAME=musafir

# Admin panel login (Phase 1 auth; replaced by Cognito for users in Phase 3)
ADMIN_USER=admin
ADMIN_PASS=admin123

# Phase 3+ (leave blank for now)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
OPENWEATHER_API_KEY=
AWS_REGION=ap-south-1
S3_BUCKET=
